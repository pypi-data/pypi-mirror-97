#!python
import os
import sys
import subprocess

if __name__ == '__main__':
    import ascend_deployer
    root_path = ascend_deployer.__path__[0]
    script = os.path.join(root_path, "start_download.sh")
    subprocess.call([script] + sys.argv[1:])
