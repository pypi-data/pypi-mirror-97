"""Plugin to serve the legacy app based on ActiveUI 4 at ``f"{session.url}/legacy/"``.

It can be installed with pip or conda:

    * .. code-block:: bash

          pip install atoti[legacy-app]

    * ..code-block:: bash

          conda install atoti-legacy-app

"""

from ._version import VERSION as __version__
