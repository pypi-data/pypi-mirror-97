#!python

import sys
from cldflex import lift2cldf

if len(sys.argv) == 2:
    lift2cldf.convert(
        lift_file=sys.argv[1],
    )
else:
    lift2cldf.convert(
        lift_file=sys.argv[1],
        lg_id=sys.argv[2],
    )