[tool.poetry]
name = "datek_app_utils"
version = "0.2.0"
description = "Utilities for building applications"
authors = ["Attila Dudas <attila.dudas@protonmail.com>"]
license = "MIT"
include = ["README.md"]
readme = "README.md"
repository = "https://gitlab.com/DAtek/app-utils"

[tool.poetry.dependencies]
python = "^3.8"

[tool.poetry.dev-dependencies]
pytest = "^6.2.1"
pytest-cov = "^2.10.1"

[build-system]
requires = ["poetry-core>=1.0.0"]
build-backend = "poetry.core.masonry.api"
