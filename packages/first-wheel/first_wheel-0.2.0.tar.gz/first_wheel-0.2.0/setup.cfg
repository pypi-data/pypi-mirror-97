[metadata]
name = first_wheel
version = 0.2.0

[options]
packages = find_namespace:
package_dir = 
	= src
install_requires = 
	docopt >= 0.6.2
include_package_data = True #MANIFEST.in

[options.packages.find]
where = src

[options.entry_points]
console_scripts = 
	test = first_wheel.first_wheel:main

[egg_info]
tag_build = 
tag_date = 0

