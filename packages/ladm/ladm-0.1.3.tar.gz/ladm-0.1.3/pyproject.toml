[tool.poetry]
name = "ladm"
version = "0.1.3"
description = "Language Agnostic Dependency Management"
authors = ["Leftshift One"]

[tool.poetry.dependencies]
python = "^3.7.7"
pyyaml = "^5.3.1"
gaia-sdk = "^3.1.0"

[tool.poetry.dev-dependencies]
pytest = "^6.2.1"
pytest-cov = "^2.10.1"

[tool.poetry.scripts]
test = "poetry_scripts:test"

[build-system]
requires = ["poetry>=0.12"]
build-backend = "poetry.masonry.api"
