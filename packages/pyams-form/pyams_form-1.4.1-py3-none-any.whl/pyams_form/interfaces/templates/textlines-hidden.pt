<input type="hidden"
	   class="hidden-widget"
       id="${view.id}"
       name="${view.name}"
       title="${view.title}"
       tabindex="${view.tabindex}"
       accesskey="${view.accesskey}"
       value="${view.value}" />