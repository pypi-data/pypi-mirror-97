<input type="hidden"
	   id="${view.id}"
	   name="${view.name}"
	   value="${view.value}"
	   class="hidden-widget"
	   title="${view.title}"
	   tabindex="${view.tabindex}"
	   accesskey="${view.accesskey}" />