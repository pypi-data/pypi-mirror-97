ENV: TESTING
PING: PONG

DISK:
    ROOT_PATH: /tmp/

S3:
    HOST: !ENV ${S3_HOST}
    PORT: !ENV ${S3_PORT}
    ACCESS_KEY: !ENV ${S3_ACCESS_KEY}
    SECRET_KEY: !ENV ${S3_SECRET_KEY}
    CALLING_FORMAT: !ENV ${S3_CALLING_FORMAT}
    BUCKET: !ENV ${S3_BUCKET}
    SECURE: !ENV ${S3_SECURE}
    ROOT_PATH: !ENV ${S3_ROOT_PATH}

S3BDL:
    URL: !ENV ${S3_BDL_URL}
    SECRET_KEY: !ENV ${S3_BDL_SECRET_KEY}
    ROOT_PATH: !ENV ${S3_BDL_ROOT_PATH}