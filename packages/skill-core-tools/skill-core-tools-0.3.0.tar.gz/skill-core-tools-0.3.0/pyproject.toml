[tool.poetry]
name = "skill-core-tools"
version = "0.3.0"
description = ""
authors = ["Leftshift One Software GmbH <devs@leftshift.one>"]

[tool.poetry.dependencies]
python = "^3.6"

[tool.poetry.dev-dependencies]
pytest = "^5.2"
pytest-cov = "^2.8.1"
toml = "^0.10.0"

[tool.poetry.scripts]
install = "poetry_scripts:install"
test = "poetry_scripts:test"
build = "poetry_scripts:build"
publish = "poetry_scripts:publish"
release = "poetry_scripts:release" # e.g. poetry run release minor

[build-system]
requires = ["poetry>=0.12"]
build-backend = "poetry.masonry.api"
