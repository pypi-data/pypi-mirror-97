Please change the following files according to your needs:

* delivery.yaml: change environment variables and Docker image - both in
    'dependencies' section and 'IMAGE' environment.
* deploy/apply/stackset.yaml: change container's environment variables and
    adapt the stack lifecycle parameters.
